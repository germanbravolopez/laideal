# Dead Code Report — La Ideal

Snapshot of methods declared but never called across `src/`. Generated by the [`dead-code-finder`](.claude/agents/dead-code-finder.md) agent (invoke with `/dead-code` or the Agent tool).

**Generated**: 2026-05-24 — commit `72208c7` (`feature/add_mdiago_verifactu`)

**Scope**: methods declared in `.h` files under `src/`, checked for callers across all `.cpp` / `.h` files under `src/`. Qt auto-connect slots (`on_*`), virtual overrides, signals, constructors/destructors, and singleton accessors are excluded.

---

## `src/tableview/mysortfilterproxymodel.h`

Date-range filter API that was never wired up — likely intended for a feature that did not ship.

| Line | Signature |
|------|-----------|
| 42 | `QDate filterMinimumDate() const` |
| 43 | `void setFilterMinimumDate(QDate date)` |
| 45 | `QDate filterMaximumDate() const` |
| 46 | `void setFilterMaximumDate(QDate date)` |

## `src/verifactu/verifactuintegration.h`

| Line | Signature | Note |
|------|-----------|------|
| 19 | `VerifactuManager *getManager() const` | Inline accessor, no callers |
| 29 | `VerifactuResult createAndSubmitInvoice(...)` | Full-invoice variant with buyer NIF/name; only `submitSimplifiedInvoice` is used (laundry retail = F2 simplified) |
| 52 | `QString getConfigInfo() const` | Wrapper over `VerifactuManager::getConfigurationInfo()`; no callers |

## `src/verifactu/verifactumanager.h`

| Line | Signature | Note |
|------|-----------|------|
| 75 | `VerifactuResult submitInvoices(QList<VerifactuInvoice>)` | Batch submit endpoint; never called |
| 101 | `QString encodeImageToBase64(QPixmap) const` | Only the `decode` counterpart is used (QR comes from API as base64) |

## `src/verifactu/verifactuconfig.h`

| Line | Signature | Note |
|------|-----------|------|
| 27 | `QString getEmitterCompanyName() const` | Set via `setEmitterData` 3rd arg but never read |
| 32 | `QString getSystemDeveloper() const` | Set via `setSystemData` but never read |
| 39 | `void load()` | Public API; config is actually loaded via `AppSettings` → `loadEmitterConfiguration` |

## `src/verifactu/verifactuinvoice.h`

| Line | Signature | Note |
|------|-----------|------|
| 34 | `VerifactuTaxItem::setDescription(...)` | No callers |
| 36 | `VerifactuTaxItem::getTaxType()` | No callers |
| 37 | `VerifactuTaxItem::getTaxRate()` | No callers |
| 40 | `VerifactuTaxItem::getOperationType()` | No callers |
| 84 | `VerifactuInvoice::setTotalAmount(double)` | Totals are computed in `calculateTotals`, never set externally |
| 85 | `VerifactuInvoice::setTotalTaxAmount(double)` | Same as above |
| 89 | `VerifactuInvoice::getInvoiceType()` | No callers |
| 90 | `VerifactuInvoice::getDescription()` | No callers |
| 95 | `VerifactuInvoice::getBuyerNIF()` | No callers (buyer info written but never read) |
| 96 | `VerifactuInvoice::getBuyerName()` | No callers (same) |
| 99 | `VerifactuInvoice::getTotalTaxAmount()` | No callers |
| 103 | `VerifactuInvoice::clearTaxItems()` | No callers |
| 130 | `VerifactuInvoice::operationTypeToString(VerifactuTaxItem::OperationType)` | Member overload; the static `VerifactuTaxItem::operationTypeToString` is the one actually used |
| 131 | `VerifactuInvoice::taxTypeToString(...)` | No callers |

## Transitively dead (only if you remove `createAndSubmitInvoice` first)

- `VerifactuTaxItem::setTaxType` and `VerifactuTaxItem::setOperationType` — their only live caller is inside `createAndSubmitInvoice`.

---

## Clean modules

No dead methods found in: `app/`, `add_garment/`, `appsettings/`, `contabilidad/`, `facturas/`, `imprimir/`, `listado/`, `logging/`, `recog_prendas/`, `sql_lite/`.

## Caveats

- The bulk of the dead code lives in `verifactu/` — the module was built with a broader API surface than what `MainWindow` + `RecogPrendas` ended up needing (F2 simplified only, no batch, no buyer fields).
- Removing dead methods is not safe without checking external consumers. This codebase is self-contained (no other repos link against these libraries), so the verifactu cleanup is low-risk if you want to do it.
- This report is a snapshot; rerun the agent to refresh it.
