# Dead Code Report — La Ideal

Snapshot of methods declared but never called across `src/`. Generated by the [`dead-code-finder`](.claude/agents/dead-code-finder.md) agent (invoke via the Agent tool with `subagent_type: "dead-code-finder"`).

**Generated**: 2026-05-31 — after the dead-code cleanup of `src/verifactu/`.

**Status**: All verifactu dead methods (19 items from the original tracker entry) have been removed as of this snapshot. The remaining entries below were evaluated as dormant/live API surface and intentionally kept.

**Scope**: methods declared in `.h` files under `src/`, checked for callers across all `.cpp` / `.h` files under `src/`. Qt auto-connect slots (`on_*`), virtual overrides (`override`), signals (`signals:` block), constructors / destructors / `setupUi()`, singleton accessors, and the free `verifactuEstado*` helpers are excluded.

---

## Remaining dormant (kept as deliberate API)

These methods are part of the public data-class API and are not dead so much as currently unused by production paths. Keeping them is a defensible design choice for a Qt data layer.

**`MySortFilterProxyModel`** (date-range filter — never wired to a `Listado` view):
- `filterMinimumDate()` / `setFilterMinimumDate()` / `filterMaximumDate()` / `setFilterMaximumDate()` / `dateInRange()`

**`FilterWidget`** (Q_PROPERTY setters — may be activated via Qt meta-object system):
- `setCaseSensitivity()` / `setPatternSyntax()`

**`VerifactuInvoice`** (retained for a future R5 workflow that could need them):
- `getInvoiceType()`, `getDescription()`, `getBuyerNIF()`, `getBuyerName()`, `getTotalAmount()`, `getTotalTaxAmount()`
- `setBuyerNIF()`, `setBuyerName()`, `setTotalAmount()`, `setTotalTaxAmount()`
- `clearTaxItems()`, `getTaxItems()`

**`VerifactuTaxItem`** (retained, same reason):
- `setTaxType()`, `getTaxType()`, `setOperationType()`, `getOperationType()`, `setDescription()`, `getDescription()`, `getTaxRate()`

---

## Caveats

- `VerifactuManager::getValidationUrl(const VerifactuInvoice&)` is public but used only internally inside `generateQRAsync`. Mirrors `VerifactuConfig::getValidationUrl` so the public placement looks deliberate — left as live. Could be demoted to private as a defensible cleanup.
- `PayDialog::onSelectionChanged()` — slot declared but never connected. The UI wires `itemChanged` to a lambda directly, so the slot is orphaned.
- This report is a snapshot — rerun the agent to refresh it.
