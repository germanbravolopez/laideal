# This file is used to ignore files which are generated
# ----------------------------------------------------------------------------
# release staging dirs (releases/<version>/, deleted by release.ps1 anyway)
releases/[0-9]*/

# build files (installer is emitted into build-release/ by release.ps1)
build*

# temporary or log files
*.bak
*.tmp
*.log

# scratch SQLite databases. Smoke testing (docs/smoke_test.md) works against
# copies of a real database - never let one be committed, they contain customer data.
*.db
*.db-wal
*.db-shm

# qtcreator generated files
*.pro.user*
*.txt.user*

# binaries
*.dll
*.exe
*.zip

# local settings
.claude/settings.local.json

# MkDocs documentation site build output (see mkdocs.yml / .github/workflows/docs.yml)
site/
_site/
